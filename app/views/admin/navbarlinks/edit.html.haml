%h2 Edit Nav bar  Link
= form_for [:admin, @navbarlink] do |f| 
	- @navbarlink.errors.full_messages.each do |msg|
		.alert.alert-error= msg 
	= f.label :name
	= f.text_field :name
	= f.label :url
	= f.text_field :url
	= f.label :position
	= f.text_field :position
	%p= f.submit 'Update', :class => "btn btn-primary"